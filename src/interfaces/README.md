# src/interfaces/

Shared TypeScript interfaces and types used throughout the project.

Examples:
- Message format (user message, agent message)
- Tool call / tool result shapes
- Orchestrator response types
- Specialist agent discovery result

Keeping types here makes the code easier to read and refactor.